<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.braintraining">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BrainTraining">
        <activity android:name=".ui.play.ColourActivity"></activity>
        <activity android:name=".ui.play.ReactionActivity" />
        <activity
            android:name=".ui.play.RiddleActivity"
            android:label="@string/title_activity_riddle"
            android:theme="@style/Theme.BrainTraining.NoActionBar" />
        <activity android:name=".ui.play.TriviaActivity" />
        <activity android:name=".ui.learn.LearnActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.play.PlayActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>